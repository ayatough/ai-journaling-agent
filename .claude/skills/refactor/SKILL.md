# リファクタリングスキル

## 手順

1. プランモードで変更範囲と目的を提示
2. ユーザー承認後に実施
3. リファクタリング前後で振る舞いが変わらないことをテストで確認
4. `uv run pytest` → `uv run ruff check src/ tests/` → `uv run mypy src/`
5. `jj describe` で変更内容を記述

## 原則

- 機能追加とリファクタリングを同時にやらない
- テストが無い部分は先にテストを書いてからリファクタ
